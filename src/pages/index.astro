---
import SubdomainForm from '@/components/SubdomainForm.svelte';
import Layout from '../layouts/Layout.astro';
import { protocol, rootDomain } from "@/utils";
---

<Layout>
	<div class="flex min-h-screen flex-col items-center justify-center bg-gradient-to-b from-blue-50 to-white p-4 relative">
      <div class="absolute top-4 right-4">
        <a
          href="/admin"
          class="text-sm text-gray-500 hover:text-gray-700 transition-colors"
        >
          Admin
	  	</a>
      </div>

      <div class="w-full max-w-md space-y-8">
        <div class="text-center">
          <h1 class="text-4xl font-bold tracking-tight text-gray-900">
            {rootDomain}
          </h1>
          <p class="mt-3 text-lg text-gray-600">
            Create your own subdomain with a custom emoji
          </p>
        </div>

        <div class="mt-8 bg-white shadow-md rounded-lg p-6">
          <SubdomainForm client:load rootDomain={rootDomain} protocol={protocol} />
        </div>
      </div>
    </div>
</Layout>
